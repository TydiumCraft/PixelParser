# PixelParser
Parsing and filtering script for TydiumCraft's PixelPolice logs.

The npm modules are only used for eslint and are not required.

This is still in a very early stage of development, so expect frequent bugs.